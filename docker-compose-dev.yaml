services:
  api_isikmedia_dev:
    hostname: api_isikmedia_dev
    container_name: api_isikmedia_dev
    build:
      context: ./
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "80"
    environment:
      ASPNETCORE_ENVIRONMENT: 'Staging'
    labels:
          - "traefik.enable=true"
          - "traefik.http.routers.apiisik_dev.rule=Host(`dev.api.isik.media`)"
          - "traefik.http.routers.apiisik_dev.entrypoints=websecure"
          - "traefik.http.routers.apiisik_dev.tls.certresolver=letsencrypt"
          - "traefik.http.services.apiisik_dev.loadbalancer.server.port=80"
          - "traefik.http.routers.apiisik_dev.middlewares=ratelimit@file,compress@file"
    networks:
      - log-net
      - traefik-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/api/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  portal_isikmedia_dev:
    hostname: portal_isikmedia_dev
    container_name: portal_isikmedia_dev
    build:
      context: ./UiPreparation/FlutterUi/flutter_devarchitecture
      dockerfile: Dockerfile
      args:
        URL: "https://dev.api.isik.media/api/v1"
        FIREBASE: "false"
        ENVIRONMENT: "staging"
    restart: unless-stopped
    expose:
      - 3000
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.portalisik_dev.rule=Host(`dev.portal.isik.media`)"
      - "traefik.http.routers.portalisik_dev.entrypoints=websecure"
      - "traefik.http.routers.portalisik_dev.tls.certresolver=letsencrypt"
      - "traefik.http.services.portalisik_dev.loadbalancer.server.port=3000"
      - "traefik.http.routers.portalisik_dev.middlewares=ratelimit@file,compress@file"
    networks:
      - log-net
      - traefik-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"


networks:
  log-net:
    external: true
    name: log-net
  traefik-net:
    external: true
    name: traefik-net